import React from 'react';
import './ListItem.css';

const ListItem = (props) => (
        <div>
            <img src={props.imageUrl}></img>

        </div>
   
);

export default ListItem;